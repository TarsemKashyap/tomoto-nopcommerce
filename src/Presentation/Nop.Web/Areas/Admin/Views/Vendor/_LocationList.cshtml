 @model VendorModel

   

    <link rel="stylesheet" type="text/css" href="https://cdn.datatables.net/1.10.12/css/dataTables.bootstrap.min.css"/>
<div>
   <table id="table" class="table table-striped table-bordered" cellspacing="0" width="100%">
    <thead>
      <tr>
        <th>Latitude</th>
        <th>Longitude</th>
        <th>Address</th>
        <th></th>
      </tr>
    </thead>
    <tbody>

        @foreach (var item in Model.GeoLocationModel)
            {
                <tr>
                    <td>@item.lat</td>
                    <td>@item.lng</td>
                    <td>@item.state <br /> @item.city</td>

                    <td>

                        <form asp-route-id="@item.Id" asp-controller="Vendor" asp-action="RemoveLocation" method="post">
                            <button new { onclick="return confirm('Are sure wants to delete?');" class="btn btn-danger" type="submit">Delete</button>
                        </form>
                </tr>
            }
    
      
    </tbody>
  </table>
</div>







 <script type="text/javascript" charset="utf-8">
    $(document).ready(function() {
    $('#table').dataTable();
    } );
    </script>


<script src="http://ajax.aspnetcdn.com/ajax/jquery.dataTables/1.9.4/jquery.dataTables.min.js"></script>